name: Greengage Reusable Behave Tests

# Environment variables
env:
  COMPOSE_HTTP_TIMEOUT: 400
  DOCKER_COMPOSE: "docker compose"

# Trigger for reusable workflow
on:
  workflow_call:
    inputs:
      version:
        description: 'Greengage version (e.g., 6 or 7)'
        required: true
        type: string
      target_os:
        description: 'Target OS for build (e.g., ubuntu, centos, rockylinux)'
        required: true
        type: string
      target_os_version:
        description: 'Target OS version (e.g., 22, 7, 8)'
        required: false
        type: string
        default: ''
      python3:
        description: 'Python3 build argument (ignored)'
        required: false
        type: string
        default: ''
    secrets:
      ghcr_token:
        description: 'GitHub token for GHCR access'
        required: true

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    outputs:
      feature_matrix: ${{ steps.set-matrix.outputs.feature_matrix }}
    steps:
      - name: Checkout Greengage repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.ref }}
          submodules: recursive

      - name: Get feature list
        id: set-matrix
        run: |
          behave_tests_dir="gpMgmt/test/behave/mgmt_utils"
          features=$(ls $behave_tests_dir -1 | grep feature | grep -v -E "cross_subnet|gpssh|gpperfmon" | sed 's/\.feature$//')
          feature_array=$(echo "$features" | jq -R -s -c 'split("\n") | map(select(. != ""))')
          echo "feature_matrix=$feature_array" >> $GITHUB_OUTPUT

  behave:
    needs: generate-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        feature: ${{ fromJson(needs.generate-matrix.outputs.feature_matrix) }}
    permissions:
      contents: read  # Explicit for default behavior
      packages: read  # Explicit for GHCR access clarity
      actions: write  # Required for cache and artifact upload
    steps:
      - name: Move Docker storage to /mnt/docker
        run: |
          sudo bash -c '
            systemctl stop docker
            mkdir -p /mnt/docker
            rsync -a /var/lib/docker/ /mnt/docker
            chown -R docker:docker /mnt/docker
            echo "{ \"data-root\": \"/mnt/docker\" }" > /etc/docker/daemon.json
            systemctl start docker
          '

      - name: Restore & Load SHA image
        uses: greengagedb/greengage-ci/.github/actions/restore-load-image@main
        with:
          version: ${{ inputs.version }}
          target_os: ${{ inputs.target_os }}

      - name: Checkout Greengage repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.ref }}
          submodules: recursive

      - name: Set up Docker Compose
        uses: docker/setup-compose-action@v1

      - name: Fetch SQL dump for ${{ matrix.feature }}
        if: matrix.feature == 'gpexpand'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sqldump_run_id=$(gh run list --workflow "Greengage SQL Dump" \
            --limit 100 \
            --json conclusion,headBranch,status,databaseId \
            --jq 'map(select(.status=="completed" and .conclusion=="success" and .headBranch=="${{ inputs.version == '7' && '7.x' || 'main' }}")) | .[0].databaseId')
          [[ "$sqldump_run_id" =~ ^[0-9]+$ ]] && \
           gh run download $sqldump_run_id --name sqldump_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }} \
           || exit 1
          tar -xf ${{ inputs.target_os }}_postgres_sqldump.tar
          rm -f ${{ inputs.target_os }}_postgres_sqldump.tar

      - name: Run Behave feature ${{ matrix.feature }}
        # continue-on-error: true
        env:
          IMAGE: ghcr.io/${{ github.repository }}/ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}:${{ github.sha }}
        run: |
          export IMAGE=${IMAGE,,}
          bash ci/scripts/run_behave_tests.bash ${{ matrix.feature }}

      # Upload feature artifacts
      - name: Upload ${{ matrix.feature }} artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}_${{ matrix.feature }}_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}_results
          path: |
            allure-results
            logs_cdw
            logs_sdw1
            logs_sdw2
            logs_sdw3
          retention-days: 7
          if-no-files-found: ignore  # Ignore if no artifacts are found

  collect-results:
    needs: behave
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read  # Explicit for default behavior
      packages: read  # Explicit for GHCR access clarity
      actions: write  # Required for artifact upload
    steps:
      # Download artifacts from behave jobs
      - name: Download feature artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: behave_*_results
          merge-multiple: true

      # Install Allure CLI
      - name: Install Allure CLI
        run: |
          # Download Allure .deb package
          curl -o allure_2.34.0-1_all.deb -L https://github.com/allure-framework/allure2/releases/download/2.34.0/allure_2.34.0-1_all.deb
          # Update package index
          sudo apt-get update
          # Install Allure and dependencies
          sudo apt-get install -y ./allure_2.34.0-1_all.deb
          # Remove .deb file
          rm allure_2.34.0-1_all.deb
          # Verify installation
          allure --version

      # Generate Allure report
      - name: Generate Allure report
        run: |
          cd artifacts
          [ -d allure-results ] && allure generate allure-results -o allure-report --clean || echo "No allure-results to generate report"

      # Upload final artifact
      - name: Upload final artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.job }}_all_ggdb${{ inputs.version }}_${{ inputs.target_os }}${{ inputs.target_os_version }}
          path: artifacts
          retention-days: 7
          if-no-files-found: warn  # Warning if no artifacts are found
